# Skill: Tutorial Step Selection

Only include steps that matter to the learner's workflow.

Do not turn incidental details into steps unless they are required:
- machine names
- remote desktop context
- one-off local setup choices
- transcript housekeeping

Select steps based on instructional importance, not because they happened on
screen.
